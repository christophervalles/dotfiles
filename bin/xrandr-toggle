#!/bin/bash
intern=eDP1
extern=HDMI2

if xrandr | grep "$extern connected (normal"; then
    xrandr --output "$extern" --mode 3440x1440 --above "$intern"
else
    xrandr --output "$extern" --off
fi

xset dpms 300 600 900

exit 0
