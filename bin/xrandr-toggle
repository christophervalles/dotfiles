#!/bin/bash
intern=eDP1
extern=DP3-2

if xrandr | grep "$extern connected (normal"; then
    xrandr --output "$extern" --mode auto --above "$intern"
else
    xrandr --output "$extern" --off
fi

exit 0
